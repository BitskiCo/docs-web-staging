---
title: "Get contract metadata"
description:
  "Contract metadata returns associated metadata for a contract for all asset
  types including fungible (ERC20), semi-fungible (ERC1155) and non-fungible
  (EC721) assets on the Ethereum and Polygon blockchains."
api:
  "GET https://api.bitski.com/v1/tokens/{coinType}/{chainId}/{contractAddress}"
---

### Path Params

<ParamField path="contractAddress" type="string" required>
  The contract address to get data done.
</ParamField>

<ParamField path="chainId" type="string" required>
  Set the network to get data from. For EVM chains, see a list of available
  chains here.
</ParamField>

<ParamField path="coinType" type="string" required>
  Set the blockchain to get data from. Right now, only EVM based blockchains are
  supported.

</ParamField>

### Query Params

<ParamField query="forceRefresh" type="string" required>
  The contract address to get data from.
</ParamField>

### Response

<ParamField body="contract" type="object" >

    <Expandable title ="show object" >

        <ResponseField name="chainId" type="string" />
        <ResponseField name="contractAddress" type="string" required/>
        <ResponseField name="coinType" type="string" />
        <ResponseField name="chainId" type="string" />
        <ResponseField name="collection" type="object" />
        <ResponseField name="decimals" type="integer" />
        <ResponseField name="symbol" type="string" />
        <ResponseField name="coinType" type="string" />

    </Expandable>

</ParamField>

<ParamField body= "error" type="object" required >
  
    <Expandable title ="show objects" >

        <ResponseField name="message" type="string" />

    </Expandable>

</ParamField>
